apiVersion: v1
kind: Service
metadata:
  name: supply-chain-app
  namespace: default
  labels:
    app: supply-chain-app
spec:
  type: NodePort
  selector:
    app: supply-chain-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080